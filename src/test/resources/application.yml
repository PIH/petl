
logging:
  file:
    name: "${PETL_HOME}/logs/petl.log"
  level:
    root: "WARN"
    org.pih: "DEBUG"

spring:
  datasource:
    driver-class-name: "org.h2.Driver"
    url: "jdbc:h2:file:${PETL_HOME}/data/petl;DB_CLOSE_ON_EXIT=FALSE;AUTO_SERVER=TRUE"
    username: "sa"
    password: "Test123"

petl:
  configDir: "src/test/resources/configuration"
